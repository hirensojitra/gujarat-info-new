import { SquareSizeDirective } from './square-size.directive';

describe('SquareSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SquareSizeDirective();
    expect(directive).toBeTruthy();
  });
});
